<div class="rouw">
   <div class="col-md-3">
    <h2>User info</h2>
    <div></div>
    <table class="table">
      <tr>
       <th>name</th>
        <th><%= @user.name %></th>
      </tr>
    <tr>
      <th>introduction</th>
      <th></th>
    </tr>
  </table>
   <div class="row">
      <%= link_to "編集", edit_user_path(@user) %>
    </div>

  <h2>New book</h2>
    <%= form_with model: @book do |f| %>
      <h4>Title</h4>
      <%= f.text_field :title %>
      <h4>Opinion</h4>
      <%= f.text_area :body %>
    <% end %>
  </div>
</div>
